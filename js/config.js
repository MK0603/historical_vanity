/**
 * config.js — プロジェクト全体の調整用パラメーターをまとめた設定ファイル
 * 
 * 今後、振り子の動きや波の立ち方を変更したい場合は、
 * 基本的にこのファイルの数値を変更するだけで全体に反映されます。
 */

export const CONFIG = {
  // ==========================================
  // 1. 物理演算・振り子の運動 (PHYSICS)
  // ==========================================
  PHYSICS: {
    // 重力を算出するための基準となる振り子の「周期」と「長さ」
    REF_T: 20.0, // 基準周期 [秒]
    REF_L: 12.0, // 基準の紐の長さ [m]
    
    // スイング時の最大振幅（どの振り子もZ軸に最大何メートル振れるか）
    TARGET_AMPLITUDE: 3.05,
    
    // 空気抵抗（0.0で永遠に揺れ続ける、数値を上げると早く止まる）
    DAMPING: 0.0,
    
    // フレーム間の最大時間差（タブ切り替え時などの計算爆発防止）
    DT_CAP: 0.05,
  },

  // ==========================================
  // 2. 水面と波紋のプロパティ (WATER)
  // ==========================================
  WATER: {
    // 波紋シミュレーションの解像度（大きくすると波が細かくなるがGPU負荷増）
    GPU_WIDTH: 256,
    
    // シミュレートする水面全体の広さ [m]
    BOUNDS: 40.0,
    
    // 波の消えやすさ (0.999=なかなか消えない, 0.90=ドロドロですぐ消える)
    WAVE_DAMPING: 0.99,
    
    // 波の伝播スピード (2.0が標準的)
    WAVE_SPEED: 2.0,
    
    // 見た目の波の高さの強調倍率 (実際の物理交差以上のダイナミックな陰影をつける)
    VISUAL_SCALE: 6.0,
  },

  // ==========================================
  // 3. 見た目・ビジュアルサイズの基準 (VISUALS)
  // ==========================================
  VISUALS: {
    // 振り子の糸(ロッド)の半径
    ROD_RADIUS: 0.022,
    
    // 重さ(mass)が 1.0 のときの球(ボブ)の基準半径
    BASE_BOB_RADIUS: 0.40,
  },

  // ==========================================
  // 4. 初期の振り子配置設定 (PENDULUMS)
  // ==========================================
  PENDULUMS: [
    { period: 18.0, xOffset: -3.5, mass: 0.4 }, // 左側: 短い周期、軽い
    { period: 20.0, xOffset:  0.0, mass: 3.5 }, // 中央: 基準周期、非常に重い
    { period: 22.0, xOffset:  3.5, mass: 1.0 }, // 右側: 長い周期、基準重量
  ]
};

// 物理定数 G（重力加速度相当）の自動計算エクスポート
export const G = CONFIG.PHYSICS.REF_L * Math.pow((2 * Math.PI) / CONFIG.PHYSICS.REF_T, 2);
