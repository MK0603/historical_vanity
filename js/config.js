/**
 * config.js — プロジェクト全体の調整用パラメーターをまとめた設定ファイル
 *
 * 今後、振り子の動きや波の立ち方を変更したい場合は、
 * 基本的にこのファイルの数値を変更するだけで全体に反映されます。
 */

export const CONFIG = {
  // ==========================================
  // 1. 物理演算・振り子の運動 (PHYSICS)
  // ==========================================
  PHYSICS: {
    // 重力を算出するための基準となる振り子の「周期」と「長さ」
    REF_T: 20.0, // 基準周期 [秒]
    REF_L: 20.0, // 基準の紐の長さ [m]

    // スイング時の最大振幅（どの振り子もZ軸に最大何メートル振れるか）
    TARGET_AMPLITUDE: 3.05,

    // 空気抵抗（0.0で永遠に揺れ続ける、数値を上げると早く止まる）
    DAMPING: 0.0,

    // フレーム間の最大時間差（タブ切り替え時などの計算爆発防止）
    DT_CAP: 0.05,
  },

  // ==========================================
  // 2. 水面と波紋のプロパティ (WATER)
  // ==========================================
  WATER: {
    // 波紋シミュレーションの解像度（大きくすると波が細かくなるがGPU負荷増）
    GPU_WIDTH: 256,

    // 水を構成する球体の半径 [m]
    SPHERE_RADIUS: 20.0,

    // 球体の自転速度 (ラジアン/秒)
    SPHERE_ROTATION_SPEED: 0.05,

    // シミュレートする波紋座標系の広さスケール [m]
    BOUNDS: 40.0,

    // 波の消えやすさ (0.999=なかなか消えない, 0.90=ドロドロですぐ消える)
    WAVE_DAMPING: 0.998,

    // 波の伝播スピード (2.0が標準的)
    WAVE_SPEED: 2.0,

    // 見た目の波の高さの強調倍率 (数値が大きいほど波が高くシャープになる)
    VISUAL_SCALE: 4.5,
  },

  // ==========================================
  // 3. 見た目・ビジュアルサイズの基準 (VISUALS)
  // ==========================================
  VISUALS: {
    // 振り子の糸(ロッド)の半径
    ROD_RADIUS: 0.012,

    // 重さ(mass)が 1.0 のときの球(ボブ)の基準半径
    BASE_BOB_RADIUS: 0.4,
  },

  // ==========================================
  // 4. 初期の振り子配置設定 (PENDULUMS)
  // ==========================================
  PENDULUMS: [
    { period: 23.0, xOffset: -8.5, mass: 1.6 }, // 左側: 短い周期、基準重量 (比率1)
    { period: 67.0, xOffset: -4.5, mass: 4.0 }, // 中央: 基準周期、重い (比率2)
    { period: 113.0, xOffset: 0.0, mass: 8.0 }, // 右側: 長い周期、非常に重い (比率4)
    { period: 53.0, xOffset: 4.0, mass: 6.0 }, // 中央: 基準周期、重い (比率2)
    { period: 17.0, xOffset: 7.5, mass: 1.2 }, // 左側: 短い周期、基準重量 (比率1)
  ],
};

// 物理定数 G（重力加速度相当）の自動計算エクスポート
export const G =
  CONFIG.PHYSICS.REF_L * Math.pow((2 * Math.PI) / CONFIG.PHYSICS.REF_T, 2);
